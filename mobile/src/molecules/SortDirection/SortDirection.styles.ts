import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    minHeight: 44,
    justifyContent: 'flex-end',
    paddingBottom: 10,
    paddingLeft: 4,
  },
  label: {
    fontSize: 13,
    fontWeight: '700',
    letterSpacing: 0.5,
  },
});
